window.addEventListener('load', function () {    function addList(number) {        let valueList = [];        let numberOfList = Number(prompt(`Какое количество пунктов будет в списке?`));        while (!numberOfList) {            numberOfList = Number(prompt(`Введите коректно количество пунктов?`, `4`));        }        for (let i = 0; i < numberOfList; i++) {            valueList[i] = prompt(`Введите содержимое пункта #${i + 1}:`);        }        let sortValue = valueList.map(elem => elem ? elem : `Значение не введено`);        let list = document.getElementById('list-item');        for (let i = 0, j = sortValue.length; i < j; i++) {            let liList = document.createElement('li');            liList.textContent = `${sortValue[i]}`;            list.appendChild(liList);        }        let time = number;        function timeOut() {            let timer = document.getElementById('timer');            timer.textContent = `Страница будет очищена через: ${time} секунд!`;            if (!time) {                let li = list.getElementsByTagName('li');                for (let i = 0; i < li.length;) {                    list.removeChild(li[i]);                }                timer.textContent = ``;            } else {                setTimeout(timeOut, 1000);            }            time--;        }        timeOut();        return valueList;    }    addList(10);});