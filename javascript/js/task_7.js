function excludeBy(array1, array2, value) {    let result = JSON.parse(JSON.stringify(array1)); // клонування об'єкта в якому буде проводитись пошук;    let arrayFindName = [];    array2.forEach(function (val) { // запис в масив всіх значень по яким проводиться пошук;        for (let key in val) {            if (Object.is(key, value)) {                arrayFindName.push(val[key]);            }        }    });    array1.forEach(function (item1, i) { // пошук по значенням першого об'єкта у відповідній властивості зі значеннями другого об'єкта;        arrayFindName.forEach(function (items2) {            if (Object.is(item1[value], items2)) {                result.splice(i, 1, ''); // заміна  об'єкта з масива на пустий рядок            }        });    });    for (let i = 0, j = result.length; i < j; i++) { // видалення пустих рядків з масива;        if (Object.is(result[i], '')) {            result.splice(i, 1);            i--;        }    }    return result;}const users = [{    name: "Todi",    surname: "Ivanov",    gender: "male",    age: 30},    {        name: "Anna",        surname: "Ivanova",        gender: "female",        age: 24    }];const users1 = [{    name: "Tod",    surname: "Ivan",    gender: "male",    age: 45},    {        name: "Anna",        surname: "Ivanova",        gender: "female",        age: 30    }];let comparisonValue = 'name';let diffValue = excludeBy(users, users1, comparisonValue);console.log(diffValue);