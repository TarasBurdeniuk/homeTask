window.addEventListener('load', function () {    let start = document.getElementById('start');    let clear = document.getElementById('clear');    let allTime = document.getElementsByClassName('allTime');    // let thisDate = 0;    // console.log(thisDate);    let interval;    let flag = false;    // let time = {    //     hours: {    //         first: 0,    //         second: 0    //     },    //     minutes: {    //         first: 0,    //         second: 0    //     },    //     seconds: {    //         first: 0,    //         second: 0    //     },    //     millisecond: {    //         first: 0,    //         second: 0    //     }    // };    function addClass() {        this.classList.toggle("pause");        if (this.textContent === 'Start') {            this.textContent = "Pause";            timerGo();        } else {            this.textContent = 'Start';            timerStop();        }    }    start.addEventListener('click', addClass);    // function count() {    //     let newDate = new Date() - thisDate;    //     console.log(newDate);    //    //     let mSec = Math.abs(Math.floor(newDate % 100));    //     let sec = Math.abs(Math.floor(newDate / 1000) % 60);    //     let min = Math.abs(Math.floor(newDate / 1000 / 60) % 60);    //     let hour = Math.abs(Math.floor(newDate / 1000 / 60 / 60) % 24);    //    //     if (mSec.toString().length === 1) {    //         mSec = '0' + mSec;    //     }    //     if (sec.toString().length === 1) {    //         sec = '0' + sec;    //     }    //     if (min.toString().length === 1) {    //         min = '0' + min;    //     }    //     if (hour.toString().length === 1) {    //         hour = '0' + hour;    //     }    //    //     allTime[0].textContent = `${hour}`;    //     allTime[1].textContent = `${min}`;    //     allTime[2].textContent = `${sec}`;    //     allTime[3].textContent = `${mSec}`;    //     time.millisecond.second++;    //     if (time.millisecond.second === 9) {    //         time.millisecond.first++;    //         time.millisecond.second = 0;    //     } else if (time.millisecond.first === 9) {    //         time.seconds.second++;    //         time.millisecond.first = 0;    //     } else if (time.seconds.second === 10) {    //         time.seconds.first++;    //         time.seconds.second = 0;    //     } else if (time.seconds.first === 6) {    //         time.minutes.second++;    //         time.seconds.first = 0;    //     } else if (time.minutes.second === 10) {    //         time.minutes.first++;    //         time.minutes.second = 0;    //     } else if (time.minutes.first === 6) {    //         time.hours.second++;    //         time.minutes.first = 0;    //     }    // }    let newTime = {        hours: 0,        minutes: 0,        seconds: 0,        mseconds: 0    };    function timerGo() {        let thisDate = new Date();        interval = setInterval(function () {            if (thisDate === 0) {                return;            }            let newDate = new Date().getTime() - thisDate;            // dif = thisDate;            // console.log(thisDate - dif);            newTime.mseconds = Math.abs(Math.floor(newDate % 100));            newTime.seconds = Math.abs(Math.floor(newDate / 1000) % 60);            newTime.minutes = Math.abs(Math.floor(newDate / 1000 / 60) % 60);            newTime.hours = Math.abs(Math.floor(newDate / 1000 / 60 / 60) % 24);            // newTime.mseconds = newDate.getMilliseconds();            // newTime.seconds = newDate.getSeconds();            // newTime.minutes = newDate.getMinutes();            // newTime.hours = newDate.getHours();            if (newTime.mseconds.toString().length === 1) {                newTime.mseconds = '0' + newTime.mseconds;            }            if (newTime.seconds.toString().length === 1) {                newTime.seconds = '0' + newTime.seconds;            }            if (newTime.minutes.toString().length === 1) {                newTime.minutes = '0' + newTime.minutes;            }            if (newTime.hours.toString().length === 1) {                newTime.hours = '0' + newTime.hours;            }            allTime[0].textContent = `${newTime.hours}`;            allTime[1].textContent = `${newTime.minutes}`;            allTime[2].textContent = `${newTime.seconds}`;            allTime[3].textContent = `${newTime.mseconds}`;        }, 10);    }    function timerStop() {        clearInterval(interval);        flag = true;    }    clear.addEventListener('click', timerClear);    function timerClear() {        // thisDate = 0;        clearInterval(interval);        start.classList = '';        start.textContent = 'Start';        for (let i = 0; i < allTime.length; i++) {            allTime[i].textContent = '00';        }    }});