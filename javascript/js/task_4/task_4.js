let number = Number(prompt('Enter a numeric'));if (!number) {    number = Number(prompt('Enter a numeric', '5'));}const fibanachi = n => {    if (!n) {        return;    }    let result = 0,        prevOne = 1,        prev = 1;    if (n < 0) {        prevOne = -1;        prev = -1;        for (let i = -1; i >= n; i--) {            prevOne = prev;            prev = result;            result = prev + prevOne;        }    } else {        for (let i = 1; i <= n; i++) {            prevOne = prev;            prev = result;            result = prev + prevOne;        }    }    return alert(result);};fibanachi(number);